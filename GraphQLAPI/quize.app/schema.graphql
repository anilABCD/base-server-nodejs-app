schema {
  query: Query
}

type Query {
  protectedSampleQuery: [QuizeCategory!]
  quizeCategories: [QuizeCategory!]
}

type QuizeCategory {
  id: ID!
  key: String!
}

type QuizeName {
  id: ID!
  name: String
  quizeCategoryId: ID!
}
