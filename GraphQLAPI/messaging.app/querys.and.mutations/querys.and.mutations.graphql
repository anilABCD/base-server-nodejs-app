schema {
  query: Query
  mutation: Mutation
}

type Query {
  test_messages: [Message]
  messages: [Message]
  message(id: String): Message
}

type Mutation {
  sendMessage(input: SendMessageInput): [Message]
}
